# 跳跳龙小游戏

一个纯 HTML/CSS/JavaScript 的轻量小游戏。

## 运行方式

直接双击 `index.html` 打开，或在目录中运行：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 操作说明

- `空格` 或 `↑`：跳跃
- 躲开障碍物，得分会随时间增加
- 本地会保存最高分（`localStorage`）
